name: Scheduler probe

on:
  workflow_dispatch:
  schedule:
    # 北京时间每天 12:05、18:05、23:55；分别对应 UTC 04:05、10:05、15:55
    - cron: "5 4,10 * * *"
    - cron: "55 15 * * *"

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Print UTC time
        run: date -u
