name: Scheduler probe

on:
  workflow_dispatch:
  schedule:
    # 北京时间每天 22:35，对应 UTC 14:35
    - cron: "35 14 * * *"

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Print UTC time
        run: date -u
